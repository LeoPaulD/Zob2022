<!DOCTYPE html>
<html lang="fr">

{% include "head.njk" %}

<body>

    <div class="wrapper">

        {% include "header.njk" %}
    
        <main>

            {% include "back-home.njk" %}

            <img src="{{ image }}" alt="">

            <h2>Affaires</h2>

            <ul class="facts">
                {% for fact in facts %}
                <li>
                    <span>
                        <a href="{{ fact.source }}">{{ fact.title }}</a>
                    </span>

                    <br/>

                    <span>
                        {{ fact.description }}
                    </span>
                </li>
                {% endfor %}
            </ul>

            <h2>Présentation</h2>
    
            {{ content | safe }}
    
        </main>

        {% include "footer.njk" %}

    </div>

</body>

</html>